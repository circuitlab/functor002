# FUNCTOR 002

**A FUNCTOR variant**

Aiming for hyper transfer

Start the local development server (with watch mode).

```bash
npm run dev

```

Serve the `public` directory to launch the application.

```bash
http-server public

```

**1. Hyper Landscape View** (`/`)
The FUNCTOR 001B landscape as a source phenomenon.

**2. Hyper Transferred View** (`/receiver.html`)
A view for large-scale emoji position transmission encoded into a WebRTC video stream.